CREATE TABLE "USUARIOS"
(
id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
name VARCHAR(30),
email  VARCHAR(50) UNIQUE,
nif varchar(10),
censusesInfo varchar(20),
pass varchar(256)
)